# xomforms-infrastructure

Terraform IaC for **Xomforms** — DynamoDB, Lambda, API Gateway, custom authorizer, S3 + CloudFront for `xomforms.xomware.com`.

See `docs/features/xomforms/PLAN.md` in the local working tree for the full spec.

## Setup

```bash
terraform init
terraform plan
```
